# Ashley AI Studio — Core Dependencies
# Install: pip install -r requirements.txt

# Core
numpy>=1.21.0
Pillow>=8.3.0
requests>=2.26.0
psutil>=5.8.0

# UI
# tkinter is built-in Python (no pip needed)

# Optional: ML/GPU
# Uncomment to install PyTorch
# torch>=1.9.0  # CPU: pip install torch
# For GPU: pip install torch --index-url https://download.pytorch.org/whl/cu118

# Optional: Vector Search & Embeddings
chromadb>=0.3.0
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0  # or faiss-gpu for GPU support

# Optional: LLM Integration
# ollama (install separately: https://ollama.ai)

# Optional: Audio
pyttsx3>=2.90
SpeechRecognition>=3.8.1
# PyAudio>=0.2.11  # Uncomment if available on your OS

# Optional: Web
flask>=2.0.0
flask-socketio>=5.0.0
python-socketio>=5.0.0

# Testing
pytest>=6.2.0
pytest-cov>=2.12.0

# Development
black>=21.5b0
pylint>=2.8.0
mypy>=0.910
